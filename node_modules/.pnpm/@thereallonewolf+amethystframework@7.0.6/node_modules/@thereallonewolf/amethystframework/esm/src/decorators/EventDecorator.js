import { cache } from "../cache.js";
export function Event(name, botCacheNumber) {
    return function (_target, _propertyKey, descriptor) {
        let bot;
        if (cache.size == 1)
            bot = cache.first();
        else if (botCacheNumber)
            bot = cache.get(botCacheNumber);
        else
            bot = cache.first();
        bot.on(name, descriptor.value);
    };
}
