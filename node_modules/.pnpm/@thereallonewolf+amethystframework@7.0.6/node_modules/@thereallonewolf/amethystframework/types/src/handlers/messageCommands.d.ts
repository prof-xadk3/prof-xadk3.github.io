import { Message } from '../../deps.js';
import { AmethystBot } from '../interfaces/bot.js';
/**
 * Handling of incoming messageCommands.
 * @param bot
 * @param message
 * @returns
 */
export declare function handleMessageCommands(bot: AmethystBot, message: Message): Promise<unknown>;
