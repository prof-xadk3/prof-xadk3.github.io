import { ActionRow, ButtonStyles, SelectOption } from "../../deps.js";
export declare class Components extends Array<ActionRow> {
    #private;
    constructor(...args: ActionRow[]);
    /**
     * Don't allow more than 5 Action Rows
     * @returns The array itself.
     */
    addActionRow(): this;
    addSelectComponent(label: string, customId: string, options: SelectOption[], placeholder?: string, minValues?: number, maxValues?: number, disabled?: boolean): void;
    /**
     * If the last Action Row has 5 buttons, create a new one, otherwise add the button to the last
     * Action Row
     * @param {string} label - The text that will be displayed on the button
     * @param style - keyof typeof ButtonStyles,
     * @param {string} idOrLink - The ID of the button or the URL of the link
     * @param [options] - { emoji?: string | bigint; disabled?: boolean }
     * @returns The object itself.
     */
    addButton(label: string, style: keyof typeof ButtonStyles, idOrLink: string, options?: {
        emoji?: string | bigint;
        disabled?: boolean;
    }): this;
}
