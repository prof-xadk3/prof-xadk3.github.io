/** Modify a guild's MFA level. Requires guild ownership. */
export async function editGuildMfaLevel(bot, guildId, mfaLevel, reason) {
    return await bot.rest.runMethod(bot.rest, "POST", bot.constants.routes.GUILD_MFA_LEVEL(guildId), { level: mfaLevel, reason });
}
