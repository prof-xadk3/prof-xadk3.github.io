import { BigString, Bot } from "../deps.js";
/**
 * Move a member from a voice channel to another.
 */
export declare function moveMember(bot: Bot, guildId: BigString, memberId: BigString, channelId: BigString): Promise<import("../deps.js").Member>;
