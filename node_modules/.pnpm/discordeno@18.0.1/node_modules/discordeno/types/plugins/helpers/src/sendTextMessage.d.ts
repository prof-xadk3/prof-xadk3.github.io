import { BigString, Bot, CreateMessage } from "../deps.js";
/** Sends a text message. */
export declare function sendTextMessage(bot: Bot, channelId: BigString, content: string | CreateMessage): Promise<import("../deps.js").Message>;
