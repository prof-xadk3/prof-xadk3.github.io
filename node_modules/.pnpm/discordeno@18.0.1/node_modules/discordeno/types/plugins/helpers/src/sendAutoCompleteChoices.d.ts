import { ApplicationCommandOptionChoice, BigString, Bot } from "../deps.js";
export declare function sendAutocompleteChoices(bot: Bot, interactionId: BigString, interactionToken: string, choices: ApplicationCommandOptionChoice[]): Promise<void>;
