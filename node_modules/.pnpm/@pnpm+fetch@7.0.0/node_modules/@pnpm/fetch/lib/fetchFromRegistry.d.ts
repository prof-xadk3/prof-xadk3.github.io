import { type FetchFromRegistry } from '@pnpm/fetching-types';
import { type AgentOptions } from '@pnpm/network.agent';
import { type Response, type RequestInfo, type RequestInit } from './fetch';
export type FetchWithAgentOptions = RequestInit & {
    agentOptions: AgentOptions;
};
export declare function fetchWithAgent(url: RequestInfo, opts: FetchWithAgentOptions): Promise<Response>;
export type { AgentOptions };
export declare function createFetchFromRegistry(defaultOpts: {
    fullMetadata?: boolean;
    userAgent?: string;
} & AgentOptions): FetchFromRegistry;
