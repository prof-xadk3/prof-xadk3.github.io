import { ApplicationCommandTypes, } from "./shared.js";
export function isContextApplicationCommand(command) {
    return command.type === ApplicationCommandTypes.Message || command.type === ApplicationCommandTypes.User;
}
