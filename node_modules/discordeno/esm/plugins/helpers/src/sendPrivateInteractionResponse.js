/** sendInteractionResponse with ephemeral reply */
export async function sendPrivateInteractionResponse(bot, id, token, options) {
    if (options.data && !options.data?.flags)
        options.data.flags = 64; // private: true
    return await bot.helpers.sendInteractionResponse(id, token, options);
}
