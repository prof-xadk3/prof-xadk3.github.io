import { createGuildSticker } from "./createGuildSticker.js";
import { editGuildSticker } from "./editGuildSticker.js";
export function stickers(bot) {
    createGuildSticker(bot);
    editGuildSticker(bot);
}
