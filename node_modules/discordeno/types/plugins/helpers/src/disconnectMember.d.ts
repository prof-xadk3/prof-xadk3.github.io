import { BigString, Bot } from "../deps.js";
/** Kicks a member from a voice channel */
export declare function disconnectMember(bot: Bot, guildId: BigString, memberId: BigString): Promise<import("../deps.js").Member>;
