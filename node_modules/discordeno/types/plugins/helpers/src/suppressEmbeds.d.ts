import { BigString, Bot } from "../deps.js";
/** Suppress all the embeds in this message */
export declare function suppressEmbeds(bot: Bot, channelId: BigString, messageId: BigString): Promise<import("../deps.js").Message>;
