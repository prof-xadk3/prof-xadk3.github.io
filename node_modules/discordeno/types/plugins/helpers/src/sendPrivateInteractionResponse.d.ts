import type { BigString, Bot, InteractionResponse } from "../deps.js";
/** sendInteractionResponse with ephemeral reply */
export declare function sendPrivateInteractionResponse(bot: Bot, id: BigString, token: string, options: InteractionResponse): Promise<void>;
