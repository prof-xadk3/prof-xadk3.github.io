"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateRequiredParameters = exports.validateMaxOptionsLength = exports.validateDescription = exports.validateName = void 0;
const nameRegex = new RegExp(/^[\p{Ll}\p{Lm}\p{Lo}\p{N}\p{sc=Devanagari}\p{sc=Thai}_-]+$/u);
function validateName(name) {
    if (name.length < 1)
        throw new Error(`The name (${name}) must have alteast 1 characters.`);
    if (name.length > 31)
        throw new Error(`The name (${name}) must have less than 32 characters`);
    if (nameRegex.test(name))
        return name;
    throw new Error(`The name (${name}) is not valid.`);
}
exports.validateName = validateName;
function validateDescription(description) {
    if (description.length < 1)
        throw new Error(`The description (${description}) must have alteast 1 characters.`);
    if (description.length > 99)
        throw new Error(`The description (${description}) must have less than 100 characters`);
}
exports.validateDescription = validateDescription;
function validateMaxOptionsLength(options) {
    if (options.length > 24)
        throw new Error(`The options  must have less than 25 elements`);
}
exports.validateMaxOptionsLength = validateMaxOptionsLength;
function validateRequiredParameters(name, description, options) {
    // Assert name matches all conditions
    validateName(name);
    // Assert description conditions
    validateDescription(description);
    // Assert options conditions
    validateMaxOptionsLength(options);
}
exports.validateRequiredParameters = validateRequiredParameters;
